# 推荐系统流水线文档

## 📋 概述

本目录包含了完整的推荐系统流水线脚本，涵盖从数据获取到用户推送的全流程自动化。

## 🚀 快速开始

### 使用主编排器
```bash
# 列出所有可用流水线
python scripts/pipeline/pipeline_master.py list

# 运行单个流水线
python scripts/pipeline/pipeline_master.py run --pipeline arxiv_cs

# 运行日常流水线集合
python scripts/pipeline/pipeline_master.py daily

# 运行周度流水线集合  
python scripts/pipeline/pipeline_master.py weekly
```

### 直接运行单个流水线
```bash
# 运行arXiv CS完整流水线
python scripts/pipeline/pipeline_arxiv_cs_complete.py

# 运行Embedding推荐流水线
python scripts/pipeline/pipeline_embedding_recommendation.py
```

## 📊 流水线详情

### 1. arXiv CS完整流水线 (`pipeline_arxiv_cs_complete.py`)

**功能**: 完整的arXiv CS论文处理链路
**调度**: 每日执行
**预计耗时**: ~30分钟

**流程步骤**:
1. **arXiv数据获取** - 获取昨天的arXiv论文数据
2. **Embedding生成** - 为新论文生成向量表示
3. **CS候选池筛选** - 筛选CS相关类别论文
4. **翻译和AI解读** - 批量生成中文翻译和AI解读
5. **用户推荐池生成** - 为活跃用户生成个性化推荐
6. **推送通知** - 发送推荐通知给用户

**输出**:
- 新摄取论文数量
- CS候选池大小
- 翻译和解读完成数量
- 用户推荐池生成数量

### 2. Embedding个性化推荐流水线 (`pipeline_embedding_recommendation.py`)

**功能**: 基于用户embedding的深度个性化推荐
**调度**: 每日执行
**预计耗时**: ~10分钟

**流程步骤**:
1. **用户Embedding更新** - 基于最新反馈更新用户向量
2. **相似度计算** - 计算用户与候选论文的余弦相似度
3. **个性化推荐生成** - 生成Top-K相似度推荐
4. **推荐结果存储** - 保存到user_recommendation_pools表
5. **推荐分析统计** - 分析推荐质量和分布

**特点**:
- 纯数学相似度计算
- 真正的个性化推荐
- 详细的推荐质量分析

### 3. 日常维护流水线 (`pipeline_daily_maintenance.py`)

**功能**: 系统健康维护和数据质量保障
**调度**: 每日执行
**预计耗时**: ~15分钟

**流程步骤**:
1. **数据质量检查** - 检查论文数据完整性和覆盖率
2. **过期数据清理** - 清理30天前的推荐池和90天前的活动记录
3. **推荐池更新** - 为活跃用户更新推荐池
4. **系统健康检查** - 检查数据库连接和关键指标
5. **性能统计报告** - 生成用户活跃度和推荐效果报告

**监控指标**:
- Embedding覆盖率 (目标: >95%)
- 翻译覆盖率 (目标: >80%)
- 推荐点赞率
- 用户活跃度统计

### 4. 会议论文处理流水线 (`pipeline_conference_papers.py`)

**功能**: 处理顶级会议论文的专门流水线
**调度**: 每周执行
**预计耗时**: ~20分钟

**流程步骤**:
1. **会议数据获取** - 从NIPS、ICML、ICLR等顶会获取论文
2. **质量筛选分类** - 按AI/ML、CV、NLP等领域分类
3. **批量内容生成** - 翻译、AI解读、信息图生成
4. **会议推荐池** - 按类别创建专门推荐池
5. **专题推荐创建** - 创建"热门"、"趋势"、"跨领域"专题

**支持会议**:
- NIPS, ICML, ICLR (机器学习)
- ACL, EMNLP (自然语言处理)
- CVPR, ICCV, ECCV (计算机视觉)
- AAAI, IJCAI (人工智能)

### 5. 用户入驻流水线 (`pipeline_user_onboarding.py`)

**功能**: 新用户冷启动和行为分析
**调度**: 每日执行
**预计耗时**: ~5分钟

**流程步骤**:
1. **新用户检测** - 检测最近7天的新注册用户
2. **冷启动推荐** - 基于热门+最新+多样化的推荐策略
3. **兴趣探索** - 分析用户初始反馈，更新兴趣画像
4. **个性化推送** - 基于兴趣推送个性化内容
5. **行为分析优化** - 分析激活率、留存率、推荐效果

**关键指标**:
- 用户激活率 (目标: >50%)
- 7天留存率 (目标: >30%)
- 推荐点赞率 (目标: >20%)

## 🔧 配置和依赖

### 环境要求
- Python 3.11+
- PostgreSQL数据库
- 必要的Python包 (见requirements.txt)

### 数据库表依赖
- `papers`, `paper_embeddings` - 论文和向量数据
- `candidate_pools` - 候选池
- `user_feedback`, `user_profiles` - 用户数据
- `daily_recommendation_pool`, `user_recommendation_pools` - 推荐结果
- `paper_translations`, `paper_interpretations` - 内容生成

### 外部服务依赖
- arXiv API - 论文数据获取
- DeepSeek API - 翻译和AI解读
- 推送服务 - 用户通知 (可选)

## 📅 调度建议

### 日常执行 (每天)
```bash
# 建议执行顺序
python scripts/pipeline/pipeline_master.py daily
```

执行顺序:
1. `user_onboarding` - 处理新用户 (5分钟)
2. `daily_maintenance` - 系统维护 (15分钟)
3. `embedding_rec` - Embedding推荐 (10分钟)
4. `arxiv_cs` - arXiv完整流程 (30分钟)

### 周度执行 (每周一)
```bash
python scripts/pipeline/pipeline_master.py weekly
```

### 使用Cron调度
```bash
# 每天凌晨2点执行日常流水线
0 2 * * * cd /path/to/backend && python scripts/pipeline/pipeline_master.py daily

# 每周一凌晨1点执行周度流水线
0 1 * * 1 cd /path/to/backend && python scripts/pipeline/pipeline_master.py weekly
```

## 📊 监控和告警

### 关键监控指标
- 流水线执行成功率
- 数据质量指标 (覆盖率、完整性)
- 推荐效果指标 (点赞率、CTR)
- 系统性能指标 (执行时间、资源使用)

### 告警规则建议
- Embedding覆盖率 < 95%
- 翻译覆盖率 < 80%
- 推荐点赞率 < 15%
- 流水线执行失败
- 数据摄取超过2天未更新

## 🔍 故障排查

### 常见问题

1. **数据库连接失败**
   - 检查数据库服务状态
   - 验证连接配置

2. **API调用失败**
   - 检查API密钥配置
   - 验证网络连接

3. **内存不足**
   - 调整批处理大小
   - 增加系统内存

4. **执行时间过长**
   - 检查数据量增长
   - 优化查询性能

### 日志查看
```bash
# 查看流水线执行日志
tail -f backend/logs/pipeline.log

# 查看特定流水线输出
python scripts/pipeline/pipeline_master.py run --pipeline arxiv_cs --verbose
```

## 🚀 扩展开发

### 添加新流水线
1. 创建新的流水线脚本 `pipeline_xxx.py`
2. 在 `pipeline_master.py` 中注册新流水线
3. 添加相应的文档说明

### 流水线模板
```python
#!/usr/bin/env python3
"""
新流水线模板
"""

def step1_xxx():
    """步骤1: 描述"""
    print("🔄 步骤1: 描述")
    # 实现逻辑
    return result

def main():
    """主流程"""
    print("🚀 开始XXX流水线")
    start_time = datetime.now()
    
    try:
        # 执行步骤
        result = step1_xxx()
        
        # 总结
        end_time = datetime.now()
        duration = end_time - start_time
        print(f"🎉 XXX流水线完成! 耗时: {duration}")
        
    except Exception as e:
        print(f"❌ 流水线异常: {e}")
        raise

if __name__ == "__main__":
    main()
```

## 📞 支持

如有问题或建议，请联系开发团队或提交Issue。
